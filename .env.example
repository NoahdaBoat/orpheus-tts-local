# Optional: LM Studio API token if your local server requires authentication.
# Copy to .env and load it in your shell, or export before starting the app.
# .env is gitignored — never commit real tokens.
#
#   export LM_STUDIO_API_TOKEN=your-token-here
#   python3 app.py

LM_STUDIO_API_TOKEN=
